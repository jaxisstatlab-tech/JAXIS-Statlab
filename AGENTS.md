# JAXIS StatLab — Agent & Developer Rules

This root configuration points to the detailed rules defined in [.agents/AGENTS.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/AGENTS.md), the canonical design system in [apps/app/docs/design-system.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/apps/app/docs/design-system.md), and the master Dashdark precision design skill in [.agents/skills/dashdark-precision-ui/SKILL.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/skills/dashdark-precision-ui/SKILL.md).

All AI coding assistants and developers MUST inspect and strictly follow [.agents/AGENTS.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/AGENTS.md), [apps/app/docs/design-system.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/apps/app/docs/design-system.md), and [.agents/skills/dashdark-precision-ui/SKILL.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/skills/dashdark-precision-ui/SKILL.md) when working in this workspace.

### Core Non-Negotiable Directives:
1. **Follow [design-system.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/apps/app/docs/design-system.md) strictly for all UI.**
2. **Anti-Double-Padding Layout Standard**: Inner dashboard pages must use `flex flex-col gap-8 max-w-7xl mx-auto pb-24 w-full animate-content-fade` without adding redundant outer horizontal padding (the shell handles viewport gutters).
3. **Modern Minimalist Loading & Anti-Double-Loading Standard**: Always use `<LoadingState variant="page" | "table" | "card" | "inline" />` with the clean, single-track Enterprise Orange arc spinner. Zero clunky boxes, pinging blobs, or sci-fi HUD slop. Never render secondary card spinners alongside page-level loaders.
4. **Button & Dropdown Styling**: Precision `rounded-[2px]`, Title Case copy, centered high-contrast loader spinner, and zero hover/focus rings on dropdown items.
5. **Phosphor Fill Icons Exclusively (`@phosphor-icons/react` with `weight="fill"`)**: Fill icons only instead of line icons across all cards, navigation, and badges to match the Dashdark X dark-mode reference. Zero emojis anywhere, no ad-hoc raw SVGs.
6. **Sans-Serif First**: Use `font-sans` for readable content. Reserve `font-mono` strictly for IDs and metrics.
7. **Plain English Copywriting**: All UI copy must use simple, everyday words. Never use jargon like `Institutional`, `Corporate`, `Bespoke`, `Treasury`, `Disbursement`, `Settlement`, `Cadence`, `Matrices`, `Parameters`, `Telemetry`, `Governance`, `Proration`, `Deliverables`. Write labels, descriptions, loading states, toasts, and buttons as if explaining to a coworker. See Section 6 of [.agents/AGENTS.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/AGENTS.md) for full banned-word list and before/after examples. Zero double slashes (`//`) in copy.
8. **Toast Notification Protocol**: Trigger standard toasts on server action mutations, 1-click clipboard copies, and file operations.
9. **Canonical KPI Cards**: All metric and telemetry cards must exclusively use `<KpiCard />` from `@repo/ui` with uppercase mono labels (`text-xs font-mono text-white/50 tracking-wider font-semibold`), bold mono metrics (`font-mono font-bold text-2xl sm:text-3xl`), unit suffixes (`text-xs text-white/40 font-mono`), and sans-serif descriptions (`text-xs font-sans text-white/50`). Zero ad-hoc raw cards or divergent typography.
10. **Philippine Peso (₱) Symbol Harmonization**: Always use `<Peso />` from `@repo/ui` or `formatPeso()` from `@/lib/formatters`. Never render raw `₱` inside `font-mono font-bold` (renders clunky/heavy monospace fallback glyph). The peso symbol must always use `font-sans font-normal opacity-85 select-none inline-block mr-0.5`.
11. **Standardized PageHeader & Navigation Breadcrumbs**: All inner pages must exclusively use `<PageHeader />` from `@repo/ui` with the canonical hierarchy: (1) Breadcrumbs starting with `{ label: "WORKSPACE", href: "/dashboard" }` via `next/link`, (2) Authoritative Title (`font-sans font-bold text-xl sm:text-2xl`), (3) Description (`font-sans text-sm text-white/60`), and (4) Right-aligned action buttons with a bottom border divider (`border-b border-white/10 pb-6 sm:pb-8`). Zero raw `<h1>` divs.
12. **High-Speed Data Retrieval & Server Component Pre-loading**: Inner operational desks must prefetch data on the server in async Server Components (RSC) and pass `initialData` into client components to eliminate initial loading delays entirely. Use in-memory server caching (`unstable_cache` + `invalidateCacheTags`) for read-heavy operations.
13. **Dashdark X Sidebar-First Identity Anchor & Zero Desktop Topbar Standard**: The desktop main canvas operates with zero horizontal topbar, allowing PageHeaders to start immediately at y=0. All user identity, role display tags, profile routing, and session sign-out controls live directly in the Sidebar footer profile card (`Sidebar.tsx`) with full collapsible icon rail support (`w-[17.5rem]` expanded $\leftrightarrow$ `w-[5rem]` collapsed). On mobile (< lg), a lean `h-14` header bar remains for the drawer trigger.
14. **Instantaneous State & 0ms Optimistic Shift Protocol**: The `DutyClockWidget` (embedded in the sidebar and mobile header) must pre-load `initialActiveShift` on the server in `layout.tsx` (RSC) to prevent flash-of-wrong-state on page loads, calculate seconds via wall-clock diff (`Date.now() - clockInMs`) to eliminate timer drift, and execute clock-in/out optimistically with 0ms visual transitions, localStorage sync, and background server execution.
15. **System-First UI Upgrade & Anti-AI-Slop Standard**: All UI refactors must follow the master standard defined in [apps/app/docs/ui-design-upgrade.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/apps/app/docs/ui-design-upgrade.md), [.agents/skills/ui-design-upgrade/SKILL.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/skills/ui-design-upgrade/SKILL.md), and [.agents/skills/dashdark-precision-ui/SKILL.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/skills/dashdark-precision-ui/SKILL.md): strictly frontend-only, zero backend/auth/database changes, anchored to canonical dark precision substrates (#010114, #01142B, #CC6600), plain English, Emil Kowalski tactile motion, and responsive mobile-first architecture.
16. **Strict Color Restraint & Anti-Rainbow Mandate**: Reduce the use of colors unless strictly necessary. Color is a scarce cognitive resource. In 90% of cases, `<KpiCard />` metric numerals must default to crisp bold white (`variant="default"`). Never render rainbow rows where 4 adjacent cards each use a different neon hue (Amber, Green, Sky, Orange). In data tables, financial figures must default to bold white; never stack cyan, green, and yellow in a single table cell. Top-level informational KPI cards must NOT render decorative icons (`icon={...}`), reserving icons strictly for active alert states where user intervention is required (e.g. `Action Required > 0`).
17. **Tactile Live Activity & Notification Badging Standard**: All live activity counters and unread alerts must combine an active live pulse beacon (`animate-ping` outer beacon + solid `#CC6600` core) and a high-contrast Enterprise Orange count chip (`bg-[#CC6600] text-white font-mono text-[10px] font-bold px-1.5 py-0.5 rounded-[2px] shadow-sm active:scale-90`). When unread count is `0`, indicators must be completely quiet and invisible. Pre-load counts on the server in async RSC (`layout.tsx`), wire 0ms local updates via window events (`jaxis:unread-count-updated`), and revalidate on tab focus.
18. **Canonical Modern Portal UX Standards**: All portals must adhere to canonical high-efficiency UX patterns: (1) Keyboard search shortcut (`/` to focus, `Esc` to clear/blur) with `<kbd>/</kbd>` keycap badge, (2) Visual 5-stage study pipeline stepper (`Proposal` $\rightarrow$ `Contract` $\rightarrow$ `Deposit` $\rightarrow$ `Analysis` $\rightarrow$ `Deliverables`), (3) Smart 1-click "Clear Filters" on empty results, (4) Multi-document lightbox navigation with arrow/bracket keys, and (5) 1-Click copy badges (`<CopyButton variant="badge" />`) for study IDs and transaction references.
19. **Canonical Dashboard Bento Architecture (Dashdark X Precision Standard)**: Primary role dashboards across all roles (Client, Statistician, QA Lead, CEO/Admin) must adopt the 2:1 Asymmetric Bento Grid (8-col focal hero card + 4-col double-stacked auxiliary cards, balanced 4-column KPI row with inline micro-pills, and lower bento split) strictly with precision `rounded-[2px]` (zero bubbly rounded-xl/2xl corners). See Section 10 of [apps/app/docs/ui-design-upgrade.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/apps/app/docs/ui-design-upgrade.md) and the master design skill [.agents/skills/dashdark-precision-ui/SKILL.md](file:///c:/Users/ROG%20STRIX/Desktop/JAXIS%20StatLab/.agents/skills/dashdark-precision-ui/SKILL.md).
